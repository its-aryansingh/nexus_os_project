apiVersion: 1

providers:
  - name: nexus-os-defaults
    orgId: 1
    folder: "Nexus OS"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
